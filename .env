# URL base da API (pulp-fiction no Render)
# Verifique o URL exato no painel do Render antes de trocar para false
VITE_API_URL=https://pulp-fiction-n3gc.onrender.com
# VITE_API_URL=http://localhost:3333

# true  → usa dados do mock.ts (padrão de desenvolvimento)
# false → usa API real (configure também no Vercel)
VITE_USE_MOCK=false
